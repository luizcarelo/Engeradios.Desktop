<Window x:Class="Engeradios.Desktop.AuditoriaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Engeradios C3 - Relatório de Auditoria de Segurança" Height="600" Width="900"
        WindowStartupLocation="CenterOwner" Background="#0F0F0F" FontFamily="Segoe UI"
        Loaded="Window_Loaded">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <!-- Correção: Tracking removido -->
            <TextBlock Text="LOG DE AUDITORIA DO SISTEMA" Foreground="White" FontSize="22" FontWeight="Bold"/>
            <TextBlock Text="Todos os acessos e modificações estão registados abaixo. Os registos não podem ser apagados." Foreground="#CC0000" FontSize="12" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Barra de Filtro Rápido -->
        <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="6" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="FILTRAR REGISTOS:" Foreground="#AAAAAA" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,15,0"/>
                <TextBox x:Name="TxtFiltro" Grid.Column="1" Height="35" Background="#2A2A2A" Foreground="White" BorderBrush="#444444" Padding="10,0" VerticalContentAlignment="Center" TextChanged="TxtFiltro_TextChanged"/>
                <Button x:Name="BtnExportar" Grid.Column="2" Content="💾 EXPORTAR PARA CSV" Height="35" Background="#444444" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="15,0,0,0" Padding="15,0" Click="BtnExportar_Click"/>
            </Grid>
        </Border>

        <!-- Tabela Profissional -->
        <Border Grid.Row="2" Background="#1E1E1E" CornerRadius="6" Padding="15">
            <DataGrid x:Name="GridLogs" Background="#1E1E1E" BorderThickness="0" 
                      RowHeight="35" RowBackground="#1E1E1E" AlternatingRowBackground="#252525" 
                      Foreground="White" HeadersVisibility="Column" AutoGenerateColumns="False" 
                      IsReadOnly="True" GridLinesVisibility="None" SelectionMode="Single">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2A2A2A"/>
                        <Setter Property="Foreground" Value="#AAAAAA"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data / Hora (Local)" Binding="{Binding DataHora, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" Width="160"/>
                    <DataGridTextColumn Header="Severidade" Binding="{Binding Severidade}" Width="100"/>
                    <DataGridTextColumn Header="Operador" Binding="{Binding Operador}" Width="150"/>
                    <DataGridTextColumn Header="Ação Executada no Sistema" Binding="{Binding Acao}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock Grid.Row="3" Text="Apresentando os últimos 500 registos em memória." Foreground="#666666" FontSize="11" HorizontalAlignment="Right" Margin="0,10,0,0"/>
    </Grid>
</Window>